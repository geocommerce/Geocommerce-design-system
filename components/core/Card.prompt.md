Base container behind service cards, stat panels, and form blocks.

```jsx
<Card surface="raised" padding="lg" interactive>…</Card>
```

Use `padding="none"` when the card leads with full-bleed media.
