The brand lockup. The mark is the supplied `assets/logo-mark.png`; never redraw it.

```jsx
<Logo size={34} src="assets/logo-mark.png" />
```

Pass `src` relative to the page you are rendering. Use `tone="inverse"` on dark footers.
